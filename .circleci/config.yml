version: 2
jobs:
  build:
    docker:
      - image: alpine
    steps:
      - run: apk add --quiet --no-cache ca-certificates
      - checkout
      - run: mkdir ~/rspec
      - run: cp report/results_junit.xml ~/rspec/test.xml
      - store_test_results:
          path: ~/rspec
      - store_artifacts:
          path: report
#  lint:
#    docker:
#      - image: koalaman/shellcheck-alpine
#    steps:
#      - checkout
#      - run: shellcheck test.sh
