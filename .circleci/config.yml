version: 2
jobs:
  build:
    docker:
      - image: alpine
    steps:
      - checkout
      - run: ls
#  lint:
#    docker:
#      - image: koalaman/shellcheck-alpine
#    steps:
#      - checkout
#      - run: shellcheck test.sh
